source 'http://gemcutter.org'

# for loading frozen gems from vendor
gem 'rails', '2.3.8'
gem 'bundler', '0.9.26'

# We need a rake dependency so rake tasks will work
gem 'rake', '0.8.7'

gem 'mysql'

group :development do
  gem 'awesome_print', '0.2.0'
  gem 'looksee', '0.2.1'
  gem 'yard', '0.5.3'
end

group :test, :development do
  gem 'factory_girl', '1.3.1', :require => false
  gem 'rspec', '1.3.0'
  gem 'rspec-rails', '1.3.2'
  gem 'test-unit', '2.0.9'
end
